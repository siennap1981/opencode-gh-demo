# OpenCode GitHub Actions Demo
Simple repo to test opencode CLI headless with free model (`opencode/hy3-preview-free`).

## Workflow
- Trigger via `gh workflow run` or comment `/oc` on issues/PRs
- Uses free opencode model + GITHUB_TOKEN (no API keys needed)
